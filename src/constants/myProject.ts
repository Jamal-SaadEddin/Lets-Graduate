export interface Partner {
  fullName: string;
  studentId: number;
  department: string;
  email: string;
  mobileNumber: string;
  address: string;
}

export interface MyProjectInfo {
  projectTitle: string;
  projectType: string;
}

export interface Supervisor {
  fullName: string;
  email: string;
  department: string;
  mobileNumber: string;
}
